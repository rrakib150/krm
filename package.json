{
  "name": "kents-repertory",
  "version": "12.0.0",
  "description": "Kent's Repertory - Homeopathic Reference App",
  "private": true,
  "scripts": {
    "build": "echo 'Static HTML - no build needed'",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
